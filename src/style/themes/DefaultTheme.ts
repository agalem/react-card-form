import { DefaultTheme } from "styled-components";

const mainTheme: DefaultTheme = {
    backgroundColors: {
        body: '#fff',
        card: '#000'
    },
    font: {
        size: '1.9rem',
        color: '#fff'
    }
}

export { mainTheme }