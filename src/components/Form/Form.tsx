import React from "react";
import styled from "styled-components";

const Container = styled.div`
    width: 700px;
    
`;

const Form: React.FC = () => {
    return (
        <Container/>
    )
};

export default Form;